# fly.toml app configuration file generated for neural-notes-embed-api on 2025-07-20T02:19:38+09:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'neural-notes-embed-api'
primary_region = 'nrt'

[build]

[http_service]
  internal_port = 8001
  force_https = false
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

  # [[http_service.checks]]
  #   interval = "20s"
  #   timeout = "10s"
  #   grace_period = "30s"  # 💡 핵심: 서버 뜨기 전엔 체크하지 마!
  #   method = "get"
  #   path = "/health"  # FastAPI에 이 경로 추가 필요

[[vm]]
  size = "shared-cpu-1x"
  memory = 1024
